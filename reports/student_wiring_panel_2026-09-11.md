# STUDENT WIRING - PANEL VERDICT (2026-09-11)

## VERDICT: LAND-WITH-CHANGES

## REQUIRED BEFORE LANDING

1. 1. NameError on the trade path (CONFIRMED, symtable: `log` is a free global never assigned; module has no `log`). sandbox_proactive_lab.py:2637 `_student_week_used(_pname, log)` -> `_student_week_used(_pname, _plog)` (the book is bound at :2484). As written, the first STUDENT_ entry reached raises before any attempt is spent, exits the loop via the `finally` at :2752 into the outer `except` at :2754 (one generic 'probe skipped (fail-open): NameError' line), and every probe after it in rotation gets zero attempts that cycle; on 6 of 11 start indices that is the whole fixed five. Also change v11_mot_harness.py:1147 which asserts the literal buggy string, and add a drill that runs run_scheduled_cycle's probe loop with a fake enabled student block and asserts a probe AFTER the student still receives attempts. Cost: 10 minutes.
2. 2. The pickers do not exist in the repo (CONFIRMED: fade_book_spec.json probe.student is null; reports/fade_meta/ has no student_*.json; src/student_features.py, scripts/student_export.py, scripts/student_asof_build.py, scripts/student_formula_sim.py, scripts/regime_drill.py are all untracked). One commit must carry, together: (a) the probe.student block with all six entries (model path, target, cohort, exit_label, k_per_week) and NO `threshold` override key — the override at sandbox_proactive_lab.py:505-506 is unit-blind, an EXPRET regressor's score is a raw return (student_export.py:60-62,72) and the ROADMAP.md:279 '0.60+' was written for the old narrow classifier; add a guard in `_pf` that rejects a spec threshold outside (0,1) for a classifier; (b) the six exported JSONs plus the search report and the sha256 of the as-of corpus they were trained on; (c) court membership: append the six names to probe.priority OR make scripts/sunday_boundary.py:358 also iterate probe.student.probes — today the court reads only probe.priority (:316-317,358) so 'same court as everyone' is false and there is no verdict clock; (d) v11_mot_harness.py:1153: FAIL, not skip, when the lab contains `def _student_score` and probe.student is absent; run the model-file / 22-feature / parity / thresholds checks whenever the block is PRESENT regardless of `enabled`; (e) scripts/fade_meta.py:200: retarget `_wired` from the literal '"STUDENT_SELECT"' (never defined by this wiring) to probe.student being present, or the owner's phone gets a false 'wiring session is owed' page weekly (telegram policy).
3. 3. Regime stand-down hoisted and fail-closed (CONFIRMED). Move the check out of `_pf` (sandbox_proactive_lab.py:500-501, which runs at :1382 only AFTER the sensor sweep at :1376) to beside the weekly-budget check at :2635, before any sweep, and stand down on None as well: `if _student_cfg().get('bear_standdown', True) and fade_book.spy_regime() in (None, 'BEAR'): continue`. fade_book.spy_regime() is fail-OPEN None on any Alpaca error (fade_book.py:47-49) and `None == 'BEAR'` is False, so on a regime-feed outage the students are the only regime-conditioned probe that keeps trading, with `reg` (from spy_dist50, :488) NaN and routed down the majority branch; the fixed probes at :2643 use `!= _rg_need` and stand down. On a BEAR day each student otherwise burns up to _PROBE_MAX_ATT=4 sweeps (:522) learning a free fact. Add the None case to scripts/regime_drill.py scenario 6.
4. 4. One sweep, six models (CONFIRMED). Six contiguous seats at _rest[0:6] (:2509-2510) draw the same _PRICEY_CANDS[:14] (:2653); `_student_score` differs only by which model it loads (:477-493); collect_metadata has no per-ticker cache (:302-317) and is paid per attempt (:1376) with attempts charged per probe (:2707-2708); len(_rest)=11 (six students + BULL_DIP, DIP_CONVEXITY, DIP_CONF_MILD, WINNER_PROFILE, FOLLOW_CALLS), so on 6 of 11 start indices the control plus two students exhaust the 10-attempt / 2-entry budget and the fixed five get nothing — this reverses the 2026-09-10 throughput fix (ROADMAP #33) the day after it landed. Fix: collapse to ONE roster seat 'STUDENT' whose filter scores all six models on the one md, enters for the best-scoring model that clears its own threshold and still has weekly budget, and stamps probe_strategy = that model's name (the court still sees six books, per-model k_per_week enforced inside). Score the whole pool first and enter the top — this also makes the live selection rule match the simulated 'best-scored first within a day' (scripts/student_formula_sim.py:200-207) instead of 'first in premium order that clears' (:2653,2661-2712). Print the per-probe attempt split in the '0 entries' line at :2743.
5. 5. Live vector honesty, minimum before enabled:true (CONFIRMED). The engine passes quote_bid=None at :486 and delta/gamma None at :490; src/student_features.py:97-98 then makes spread_frac NaN, :79-80 make delta_prev/gamma_prev/abs_delta_prev NaN, :102 makes price_drift NaN by construction, while scripts/student_asof_build.py:40-46,57 populate all five in training — the live row is outside the training support on every cycle and the k thresholds were computed on full vectors. Do, in order: (a) store `_qb` (already computed at :1963 for the 2% check) as `alert_bid` in the aggx dict at :1981-1991 and pass it at :486; (b) drop from FEATS every feature the live path cannot supply or measures as a different object: price_drift, delta_prev, gamma_prev, abs_delta_prev, and n_prints / cum_size / vol_oi_asof (archive = prints-to-the-50k-crossing, live = the UW alert cluster's trade_count/total_size); (c) retrain on the reduced list; (d) have the exporter write a per-feature training NaN-rate manifest and make `_student_score` return None with a loud line when any feature with training NaN-rate < 1% is NaN live (fail-closed); (e) have the exporter ASSERT parity_max_err <= 1e-4 and refuse to write the file otherwise (student_export.py:79-87 computes it and never checks it); (f) rewrite the MOT parity fixture (v11_mot_harness.py:1128-1136, which hand-builds the alert from the archive's own numbers, passes bid=5.0 the engine never passes, and skips NaN pairs) to drive `_student_score` on a frozen md + candidate with the engine's exact call shape and FAIL on any feature NaN on one side only, plus a support check that live cum_prem sits inside the training q01-q99.
6. 6. Label leak in reg / sp / smd — fix before any picker places an order (CONFIRMED in code; the VPS tercile statistics are not reproducible in this checkout but the mechanics are unambiguous). scripts/probe_tuner.py:63-66 `smad` computes o[x] with day x's own close inside the window; :162 keys smd/reg/sp on the ENTRY day; entry is the ask at the qualifying print + CYCLE_DELAY_MIN (:128), so the same-day close move — post-entry, part of the label — sits inside the three most-split features. Live feeds intraday running values instead (fade_book.py:37,44 `cl[-1]`; sandbox_proactive_lab.py:146 `closes[-1]`; sandbox_v11_sensors.py:58-62): a different quantity, so the leaked part of the backtest cannot transfer and the formula-search winners were chosen on the leak. This is Adversarial Architect check (4) data honesty — a RED FLAG that must be raised before the model trades. Fix: recompute the three columns at D-1 close (shift smad by one day; no replay needed — it is three columns from closes_series), rebuild student_asof with the reduced FEATS from item 5, re-run the 72-config search and the walk-forward OOS scores on the honest columns, re-export with thresholds calibrated on the OOS stream's trailing 60 sessions (student_formula_sim.fit_stream already yields it; student_export.py:68-78 currently uses in-sample tail scores, which the search never did). Live: drop the last bar when its date is today in both builders so both sides are the D-1 close; MOT: assert no regime input carries today's date. The export must print each picker's walk-forward AUC on the honest columns. This is a weekend VPS machine-time job, not a corpus replay; if it is not done by Sunday night see owner decision 3.
7. 7. Cheap fail-closed guards, same commit (CONFIRMED by structure). Wrap the roster comprehension at :2509 and `int(_scfg.get('k_per_week', 3))` at :2637 in their own try, degrading to 'no student probes this cycle' with a loud line — today a list-shaped or scalar student block raises inside the outer try at :2479 and takes EXEC_BASELINE down with it via :2754, and a non-numeric k aborts the whole loop. Wrap `_pf`'s body in try/except that prints and returns False: `float(thr)` at :510 raises inside enter_proactive_set, which :2713-2714 swallows with `continue` and no log line AFTER the attempt was charged at :2707-2708 — the exact silent-burn pattern in BREAKDOWNS for 2026-09-02. Reset `_STUDENT_LAST` at the top of each attempt, and stamp `student_model` (:2734) with the model file basename plus sha256[:8] of its bytes instead of trained[:10], so two same-day exports are distinguishable in the calibration audit.

## ACCEPTABLE FOR NOW (with conditions)

- A. Training cohort selected on end-of-day facts (CONFIRMED: scripts/probe_tuner.py:152-158 admits a contract-day only if its EOD total_premium is in [50k, 1M], EOD ask_volume > bid_volume and EOD NBBO spread <= 2%; student_asof_build.py:22-24 inherits that row SET). The live pool is calls-only, alert-level, top-14 by premium (sandbox_proactive_lab.py:1961-1977, :2044), so training is a survivor-conditioned, both-sides population and P is optimistic (side is a constant +1 live). Bounded by k_per_week, per-day 5 and 2 per cycle. CONDITIONS: (1) passively log the full live vector + score for EVERY scored candidate, harvest-style — an append after `_pf` returns, fully wrapped, never inside the order path — and publish week-1 live marginals vs training marginals in the Friday scoreboard; (2) the retrain on an as-of-selected, calls-only cohort (as-of cum_prem >= 50k, as-of ask_share > 0.5 mirroring the _asp > _bsp gate at :1972, print-level spread <= 2%) happens before any promotion verdict counts. Re-run test_harvest_passivity.py after adding the logging.
- B. iv_prev / oi_prev timing mismatch (CONFIRMED: archive iv_prev = D-1 EOD implied_volatility from contracts_daily, student_asof_build.py:40; live = the alert's intraday iv_start, :490; OI semantics asserted 'same quantity' in design section 3 without evidence). One-directional shift on a heavily-split feature. Fix after landing: pull D-1 close IV/OI via the UW contract endpoint for the <= 14 scored candidates per cycle (inside the 40k/day budget, ~6k used) or switch the archive to the print-time IV; until then state it in the model card and watch the logged live iv_prev marginal from condition A.
- C. Teacher-freeze is prose, not code (CONFIRMED: student_export.py:87 overwrites reports/fade_meta/student_<NAME>.json in place; max_model_age_days 120 at sandbox_proactive_lab.py:465-466 is a stand-down at day 121, not a pin; no probe.tuning.<name>.applied stamp is written so a re-export does not restart the court's evidence clock at scripts/sunday_boundary.py:356-366). Acceptable for the first case because nobody will re-export mid-case if told not to; fix in the next commit: dated files + pinned filename in the spec (design section 5), export writes probe.tuning.<name>.applied = trained date, export refuses while a case is open unless STUDENT_FORCE_REFIT=1 by owner order, court also splits on the record's student_model stamp.
- D. Flow-block feature definitions that survive item 5 (cum_prem, prem_oi_asof, ask_share) are still not the same object on both sides (archive = day-cumulative at the first 50k crossing, median ~75k; live = the alert cluster's total_premium after max-per-ticker in 50-400k; ask_share ties counted as ask in scripts/uw_flow_prints.py:103 vs ask/(ask+bid) live). Supports overlap, so this is a calibration drift, not a blackout. Fix after: define each feature once in alert-level terms and build the archive side by replaying UW's aggregation window on the prints; the live-vector log from condition A tells you how far the marginals sit apart before you spend the replay.
- E. Misleading comment at sandbox_proactive_lab.py:447-449 ('the roster runs whether or not the fade BOOK is on') — DISMISSED as a behavioural finding: the entire probe section is gated on fade_book.active() at :2480, so students go dark together with every other probe when the book is OFF; _PRICEY_CANDS being inside the same gate at :2036-2044 changes nothing. Fix the comment. Likewise the stale `_STUDENT_LAST['p']` claim is not a trade-path defect (the stamp only fires on entry and entry requires `_pf` True, which sets p immediately before); the reset in item 7 is hygiene.
- F. Not verifiable in this checkout (no data/uw_history.db, no hourly_paths.db, no as-of corpus): the VPS-side numbers cited by the parity lens — greeks 29.6% NaN co-missing with OI, split counts, tercile sign-flips, walk-forward picks 17 vs 1, and the claim that the picker's OOS AUC after 2026-06-01 is 0.460 even with true features. The code mechanics behind each are confirmed above; the numbers stay flagged as unverified. The re-export in item 6 must print the walk-forward AUC per picker on the honest columns so this stops being hearsay.

## OWNER DECISIONS

- 1. Court unit for the six. Verified arithmetic: the court (scripts/sunday_boundary.py:327-336,382-401) keys on shared ENTRY days, closed legs only, symmetric-trims n-2 and needs t >= 1.8 plus own-mean >= +3/day plus both halves positive; only names ending _W get the weekly unit (:369-379). At <= 3 picks/week per name that is 3-8 weeks to 8 shared days per picker, and t >= 1.8 on 6 trimmed diffs with a ~60-pt per-trade sigma needs roughly +44/day — the cited +12%/trade edge has about 1-in-7 power, while six correlated siblings partitioning one candidate list give a 7-33% chance of crowning noise. Options: (a) one STUDENT family book for the court (weekly unit, family k=18/week, placebo bar from the placebo_t95 machinery at :83-94), per-model split published as diagnostics — RECOMMENDED; (b) six individual day-unit books as ordered, accepting the court is a catastrophe detector, not a skill detector, at this cadence; (c) give each picker the _W weekly unit.
- 2. Six configurations vs the pre-registered protocol. scripts/student_formula_sim.py:10-11 pre-registered that the holdout is touched ONCE by the single winner; six of 72 configs are going live, so five carry only selection-biased search-window numbers and the family has a multiple-testing history the court is not told about. Options: (a) audition all six as ordered, with 'holdout: not touched' and search-window rank stated per picker in the roster note; (b) audition the pre-registered winner plus the one least-correlated sibling (best PWIN/BASE and best EXPRET/WIDE), keep the other four as shadow books (scored and logged via condition A, no orders) — RECOMMENDED; (c) audition the winner only.
- 3. Monday path if the honest re-export (required item 6) is not finished by Sunday night. Options: (a) land the wiring, the passive live-vector logging, the MOT and drill on Monday with probe.student.enabled=false — shadow mode, no orders — and flip enabled:true the night the leak-free models land; week-1 live feature marginals accrue for free — RECOMMENDED; (b) go live Monday on the current leak-trained models at $1,000 paper with the court clock NOT started and every fill stamped model_version=v0_leaky, treated as a plumbing shakeout, never as evidence; (c) slip the whole landing until the re-export is done. Note the roster oxygen cost of (b): even with the one-seat design the block takes attempts from the fixed five the owner just protected.
- 4. Automatic pull rule. Should a picker whose walk-forward AUC on the honest columns (printed by the re-export) is <= 0.50, or whose EXPRET OOS mean is <= 0, be pulled from the block before it trades? RECOMMENDED: yes, automatically, with the number in the roster note; the alternative is to audition it anyway as a control for the court.
- 5. Roster size vs ROADMAP #33. The owner fixed the roster at SEVEN on 2026-09-10 for throughput. The one-seat design in required item 4 makes it eight and costs one probe's attempts; six separate seats make it thirteen and cost the fixed five roughly half their cycles. Confirm the one-seat design is acceptable as the implementation of 'six pickers'; if six separate seats are insisted on, accept the throughput regression explicitly and have it printed in the '0 entries' line every starved cycle.
