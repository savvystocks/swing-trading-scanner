# INSTRUMENT MISMATCH - PANEL VERDICT (2026-09-09)

## VERDICT: REDESIGN

## FALLBACK RULING

NEITHER (a) NOR (b) — ship (c): register the trigger path as ONE new named strategy with its own pool, priority slot and virgin-day clock; leave the six incumbents' entry path byte-identical.

Why (a) fails — verified, not argued. I independently reproduced the throughput funnel on the three NBBO-bearing snapshots in data/cache_uw and matched the throughput lens exactly (raw->non-index->calls->prem 50k-1M->ask-aggressor->spread<=2%->DTE>=7->ask<=$10): 2026-06-02 493/310/279/151/58/51/10 rows = 9 tickers; 2026-06-08 467/266/224/116/38/26/5 = 3 tickers; 2026-07-01 272/161/87/42/13/9/3 = 3 tickers. Archive-eligible cohort n=109, survivors n=18 — the lens's counts reconcile precisely. Then the live gate's own floor bites: 7 of 18 survivors (39%) price under $4.00 and are guaranteed to fail `_blo <= _la <= 9.9` because the trigger leg carries no band_lo (sandbox_proactive_lab.py:450-455) and inherits the 4.0 default (:1347, :1349). Usable tickers after the floor: 5 / 3 / 1. On the live-like 07-01 snapshot the entire six-strategy roster would contest ONE ticker. Against that, _PRICEY_CANDS — already a trigger-identity pool built from the same filter family (:1828-1850) — has an observed conversion of 1 fill in ~76 (confirmed in reports/research/capture_ratio_2026-09-09.md at origin/main f81fb20f). Trigger-only restarts six 8-day clocks onto a path measured at ~1.3% conversion. That is not "cleanest evidence, costs throughput"; it is zero evidence.

Why (b) fails — four counts, all verified. (i) NO READER: scripts/sunday_boundary.py:325-336 buckets solely on `probe_strategy`; `occ_source` is written at sandbox_proactive_lab.py:1202/1207/1209/1218/1317 and read by exactly two assertions in scripts/regime_drill.py:80,99 — nothing in the scorer. Ship (b) with the drafted coverage and the two streams silently POOL under one name. (ii) NO MULTIPLICITY CONTROL: six strategies x two streams = twelve draws at a fixed `t >= 1.8` one-sided bar, re-evaluated weekly against one shared control (sunday_boundary.py:393-400); no correction exists in the file. (iii) ENDOGENOUS ASSIGNMENT: stream membership is decided by whether the live repricing gate passes (:1349) — a liquidity filter — so the trigger stream is a liquidity-selected subsample and the synth stream its complement; neither estimates the strategy. (iv) NO CLOCK: the probe court's only exclusion is `probe.tuning.<name>.applied` (sunday_boundary.py:355,363-368); the blanket clocks read `auto_*` markers (:209,:284). A code-only edit writes neither, so §5's "their virgin-day evidence restarts" is decorative — pre-change synthesized days keep counting into the same t-test.

Why (c) works: one name = one stream = one clock, so the laundering surface disappears without needing an occ_source reader in the court (the strategy name IS the tag); the incumbents keep their entry path and their accrued days, so no clock restarts and no throughput is spent; and the trigger path's low fill rate becomes a measured fact about that strategy rather than a starvation of six. Note MOT coverage for this is nil today — v11_mot_harness.py contains no reference to trigger_contract, _PROBE_CONTRACT, occ_source or band_lo, so the design's "the existing guards still fire on the trigger path" is unbacked; every fixture is new work.

## REQUIRED BEFORE BUILD

1. PULL FIRST — the reviewed checkout is stale. Local HEAD 0b8fd4de is 20+ commits behind origin/main 691be3e8, missing the roster cull 60da0f7d and the capture study f81fb20f. Every cited line verifies byte-identically on origin/main (I spot-checked :450-455, :1347, :1349, :1828, :1903) and the 5,154-line diff is CRLF noise — real delta is 442 lines — but an implementer editing the local tree tonight will fight a merge and may re-add culled strategies. Pull before touching anything.
2. STRIKE the premise sentence. reports/instrument_mismatch_design_2026-09-09.md:21 says _TRIG_CANDS uses 'the SAME filters the archive corpus used'. False. probe_tuner.py:103-108 selects only on total_premium 50k-1M, ask_volume > bid_volume, and NBBO present — there is NO expiry predicate and NO ask predicate anywhere in build_rows; :116-117 computes spread on MID, while the live filters use ASK (sandbox_proactive_lab.py:1843, :1349). DTE>=7 and ask<=$10 are live-only inventions. Measured: 18 of 109 archive-eligible rows survive both live cuts (16%). The whole build rests on that one sentence and it is not true.
3. REBUILD THE CORPUS ENTRY BASIS BEFORE ANY ENTRY-PATH CHANGE — this is the blocker that outranks the rest. probe_tuner.py:128 sets `e = today_after[0][2] if today_after else ask`: the CLOSE of the first hourly bar OPENING after the print (:126-127 filters `ts[11:19] > pts[11:19]`), from Alpaca OPRA TRADE aggregates (hourly_library.py:44 /v1beta1/options/bars, :70-71 bars table). So entry is a last-sale print roughly 60-120 minutes after the signal — not an ask, not executable, in direct violation of the house rule that entry_ref = ask at signal. Same defect at glide_sim.py:103-107 and grand_retest.py:130-133 (whose docstring at :3-4 calls it 'what the live engine really does' — it does not). Three compounding faults: (1) `today_after` INCLUDING the entry bar is passed to replay_true (:134), whose first loop (:59-62) arms the trail and sets the peak on highs that occurred BEFORE the entry close — one-directional look-ahead; (2) exits fill at synthetic levels, `return fl` and `return stop` exactly (:71-73), never at a bid, never gapping through; (3) the correct input is already banked and thrown away — uw_flow_prints.py:96-101 stores per-print executed_at AND nbbo_bid/nbbo_ask, its docstring (:3-6) states the point was 'entry at the TRUE trigger time and TRUE ask', yet every consumer takes only `min(executed_at)` and discards the ask (probe_tuner.py:90, glide_sim.py:77, grand_retest.py:100, mild_conf_liveband.py:89, mild_conf_split.py:89). Fix: set e = flow_prints.nbbo_ask at min(executed_at), drop the contract when no ask exists (do not fall back to the daily quote), pass today_after[1:] to the peak loop, and haircut trail/stop fills to the bid side. Caveat for the implementer: the stored value prefers ewma_nbbo_ask (uw_flow_prints.py:100), a smoothed NBBO — still vastly closer to executable than a trade-bar close 90 minutes later, but label it as EWMA in the report, do not call it 'the ask'.
4. FIX THE DESIGN'S CORROBORATION — it is a cherry-pick, and I read the source. reports/research/capture_ratio_2026-09-09.md DOES exist (origin/main f81fb20f; the throughput lens called it missing only because the local checkout is stale — dismiss that half of the finding). Reading it strengthens the objection: EXEC_BASELINE's row is `11 shared days | live mean +26.90 | arch mean +1.84 | GAP +25.05 | live med +5.70 | arch med +4.67 | GAP med +1.03`. The design quotes ONLY the median gap. The MEAN gap is +25.05 — above the script's own ~10-point noise floor, in the POSITIVE direction, i.e. the live sample is dominated by a monster trade (the report says the median columns exist precisely to 'resist a single monster trade'). Meanwhile the same table shows CONSENSUS at GAP -20.59 over 11 non-THIN shared days — also above the floor, pointing the opposite way — and the WEIGHTED line reads 'live -0.15%/day vs archive -0.14%/day'. capture_ratio.py:126-130 declares no gap under ~10 points distinguishable and calls itself 'a measurement instrument that is not yet loaded'. Its CAVEATS block already concedes the exact defect above: 'Live entries are at the ask; archive entries use the post-print bar close.' Strike design lines 15-17. Ship the change on the occ_source audit (1 of 76), which is direct and sufficient; do not ship it on +1.03. Also guard capture_ratio.py:51 — it opens reports/research/glide_fine_rows.jsonl unguarded and that file is tracked nowhere on origin/main, so the study cannot be reproduced from the repo at all.
5. THE AFFORDABLE SLICE IS BIMODAL, NOT ONE INSTRUMENT — I reproduced the entry lens's geometry and it is right on the numbers but wrong on the mechanism, which changes the fix. On the same three snapshots: survivors n=18 median moneyness +10.51% OTM, median DTE 45, median ask/underlying 2.41%; dropped n=91 median +2.04% OTM, median DTE 45, median ask/underlying 5.65% — matching the lens. But the survivor detail shows two disjoint populations, not one shifted one: six survivors sit at +0.13% to +1.59% OTM on 9-16 DTE (QQQ, GLD, INTC, QQQ, SPY, AAPL — near-ATM, cheap because short-dated), and a tail sits at +12% to +90% OTM on 45-227 DTE (AMZN, GOOGL x3, NOK x2, AA, IREN — cheap because miles out). The medians coincide at DTE 45 only because the two modes cancel. So the lens's claim 'not a nearer expiry — a strike six times further OTM on the same clock' is WRONG; it is both, in two modes. Consequence for the fix: a single re-derived ask<=$10 cell is not enough, because it averages a ~0.5-delta 10-DTE ETF call with a ~0.10-delta 227-DTE small-cap lottery ticket. The exit grid makes this fatal — probe_tuner.py:32-33 and :133-135 fitted -50/+50/0.20 and -70/+80/0.30 on the unfiltered cohort, and a -50% option move means a completely different underlying move in each mode. Publish the slice split by mode (DTE band x moneyness band), not as one number.
6. MAKE THE BAND ONE CONSTANT AND ENFORCE THE CAP DIRECTLY — there are currently THREE ceilings for one constraint and the cap is held up by coincidence. Pool admits price 4.00 < pc <= 9.00 (:1828); repricing gate admits ask 4.0 <= _la <= 9.9 (:1349); the design proposes ask <= $10 (doc:24). I verified the arithmetic at :1354 `round(round(_la*1.01*20)/20, 2)`: ask 9.90 -> limit 10.00 -> notional exactly $1,000.00 (ZERO headroom vs NORTH_STAR.md:38); ask 9.95 -> $1,005 BREACH; ask 10.00 -> $1,010 BREACH. So implementing the doc's `ask <= $10` and aligning the gate to it — the obvious consistency edit — silently breaches the per-trade cap on six strategies. Nothing catches it: route_to_alpaca_paper (:1429) and _order_payload (:511-518) submit whatever limit_price the leg carries, and alloc_usd records LEG_BUDGET (:454, :1435), not actual spend. Required: derive band_hi from the cap (floor_to_nickel(CAP/100/1.01) = 9.90), have pool, leg and gate all read one module-level constant pair, stamp band_lo explicitly in build_legs and DELETE the 4.0 default so an unstamped leg is a hard error, add an unconditional pre-submit assertion `contracts * limit_price * 100 <= 1000` that skips-with-reason rather than routing, and stamp notional_usd on the leg. (I checked and am dismissing one adjacent concern: the pool bands on `price` while the gate bands on `ask`, but only 2 of 298 in-band alerts had ask > 9.9 — 1%, immaterial.)
7. STAMP THE KEYS THE ENTRY PATH READS, OR ASSERT AT BUILD TIME — two live guards are already dead on the aggx row shape and will stay dead on _TRIG_CANDS. (a) The free hopeless-check `c.get('tight_spread_pct', 0) > 10.0` (:2515) — the throughput protection added 2026-09-09 for this exact attempt-budget problem — reads a key set only on `agg` rows (:1859) and never on aggx rows (:1846-1850), so `.get(...,0)` returns 0 and it never fires on the trigger pool. (b) The afford_fallback reads `afford_call`/`afford_put` (:1300-1303), set only on agg rows (:1869-1880), so on the trigger path the fallback is structurally unreachable — harmless today only because contracts=1 (:453) clears min_ct (:1294, fade_book_spec.json). Flip min_contracts to 2 and every trigger fill dies as 'premium too rich' with no alarm: a single-config fuse under the whole fix. Also add a `trigger_reprice_band` case to _skip_code (:1447-1464, which has no trigger branch, so these skips fall to 'other_engine_skip' and are never propagated at :2548 — each of six strategies re-pays the full sensor sweep for the same dead ticker), and split the aggregated reason string at :1351 into distinct codes for no-quote / crossed / spread>2% / outside-band, which is why this starvation class is invisible in the logs.
8. DO NOT PUT WINNER_PROFILE OR WINNER_PROFILE_X ON A CALLS-ONLY PATH. build_legs' trigger branch returns exactly one leg, hardcoded `structure: LONG_CALL` (:450-455), and returns before the regime router (:507). That is fine for the six names in _CALLS_ONLY (:2470-2471) — but WINNER_PROFILE and WINNER_PROFILE_X are deliberately excluded from that set, and WINNER_PROFILE is the frozen owner control documented in-code as 'Regime-agnostic, both sides' from all 25,597 labeled trades (:2413-2423); WINNER_PROFILE_X's filter (:2424) carries no side term. They draw _FULL_CANDS today, built from `agg` rows carrying call OR put (:1886-1888). _TRIG_CANDS is calls-only by construction (:1828). Routing them onto it makes the frozen secondary control calls-only in every regime, including BEARISH tape, judged against a both-sided archive cohort — the mismatch fix injecting a fresh mismatch into the one strategy whose job is to be stable, and invisible after the fact because the record still logs the classified regime (:1271, :1430-1434) while the leg is always a call. Either build the put half of the pool with mirrored filters and let the branch pick the side from flow_type (the afford_fallback already does exactly this at :1309-1310), or leave both off the trigger path and mark their cells not-live-comparable.
9. CLOSE THE SUBMITTED-BUT-UNLOGGED WINDOW BEFORE ADDING SIX MORE CALLERS. enter_proactive_set routes at :1429, then calls _notify at :1441, then _append_log at :1443 — and _append_log deliberately RAISES when the log exists but will not parse (:1470-1476, the fail-closed 2026-08-25 fix). The probe loop swallows every exception from that call with a bare `except Exception: continue` printing nothing (:2539-2540), and the outer handler prints only a type name (:2571-2572). Result: a live paper position with no record — no leg_exits, no backstop, invisible to the occ guard's OPEN scan (:1365-1369) and to _open_tk, permanently outside the one-record-per-contract invariant. Note `probe_strategy` is not even attached until _rewrite_last at :2557, widening the window further. Six trigger strategies each add a synchronous timeout=15 quote call (:1338-1346) against a 10-attempt budget (:2483) inside a job with timeout-minutes: 8 — the same envelope whose overrun is documented in-code at :2472-2479 as the cause of the 17:40Z cancellation churn; a hard timeout landing between :1429 and :1443 produces the same orphan with no exception at all. Required: write a PENDING intent record BEFORE routing and flip to OPEN after the fill response; replace the bare except with one that prints and notifies when the raise came after submission; measure a six-strategy cycle against the 8-minute timeout before shipping. Separately, the occ guard's stated invariant is 'one record per contract, EVER' (:1359-1362) but it scans only status == 'OPEN' plus broker positions (:1366-1371) — once a record closes the same occ is buyable again, which on a 14-row one-per-ticker pool means a popular monthly contract gets re-bought under a different strategy label and two 'independent' streams contain the same instrument.
10. RETARGET THE EXIT CONFIG IN THE SAME COMMIT OR THE MISMATCH JUST MOVES. The design's Coverage section (doc:43-48) never mentions scripts/tuner_apply.py:36-38, which pins INCUMBENT_POOL = FOLLOW_CALLS->fullband, CONSENSUS_CALLS->fullband, BULL_DIP->base_cheap, DIP_CONVEXITY->base_cheap, DIP_CONF_MILD->pricey_4_9. Two consequences. (i) Post-change, FOLLOW_CALLS and CONSENSUS_CALLS would BUY from an ask-capped pool while the Friday glide keeps tuning their stop/trail on a price-unrestricted cohort — the instrument mismatch relocated from entry to exit. (ii) Worse and true TODAY, independent of this build: BULL_DIP and DIP_CONVEXITY have their exits tuned on `base_cheap` = 0.30 <= ask <= 4.00 (probe_tuner.py:157), a band that sits ENTIRELY BELOW the live repricing floor of $4.00 (:1349). Those two strategies' exit configs are fitted on a cohort the live book structurally cannot buy. Fix that in this commit regardless of which fallback ships.

## OWNER DECISIONS

- SHIPPING ORDER — my recommendation, needs your word. Split into three commits and ship only the first this week: (1) COMPARATOR + CORPUS, no entry-path change, no clock restart, zero throughput cost — rebuild the corpus on the executable ask basis, drop the entry bar from the peak loop, haircut exits to the bid, then re-derive every published cell for the six strategies on the affordable slice SPLIT BY MODE, and mark the current published numbers superseded in the same commit. (2) HYGIENE — the band constant, the cap assertion, the stamped keys, the PENDING intent record, the tuner_apply retarget. These are defect fixes that stand on their own merits whatever you decide about entry. (3) ENTRY PATH, only if (1) still shows edge. Commit (1) is the only step that can be evaluated in isolation and it is free.
- THE FALLBACK QUESTION AS I WOULD PUT IT TO YOU. Not (a) vs (b). Choose between: [A] Register the trigger path as ONE new strategy with its own name, pool, priority slot and virgin-day clock; the six incumbents are untouched and keep their accrued days. Costs one roster slot, no laundering surface, no clock restarts, and the ~1-in-76 fill rate becomes that strategy's measured property. [B] Do nothing to the entry path at all; re-derive the comparators and mark the six 'not-live-comparable' where their affordable cell fails the bar. Honest, free, and costs nothing since none has 8 closed days. [C] Ship (b) with Bonferroni (t>=2.4 at twelve draws), a written statement that the trigger stream is a liquidity-selected subsample, and an explicit entry_path clock in fade_book_spec.json. I recommend [A]; I will not sign [C], because the endogenous stream assignment means neither stream estimates the strategy no matter how the bar is corrected.
- PRE-REGISTER THE BAND BEFORE ANYONE LOOKS AT THE ANSWER. The precedent this whole design generalizes from is the +21.2%/day t+4.31 `inband_expensive` cell cited verbatim at sandbox_proactive_lab.py:2502-2504 — and I checked its definition at mild_conf_split.py:94-97: `('inband_expensive', 50000, 400000, 4.00, 1e9)`, ask $4 to INFINITY, no cap. Nobody has ever measured the ask<=$10 slice of it. The affordability conditioner is also non-monotone and large: on the same DIP_CONF_MILD cell, fullband is +11.2%/day t+2.63 while the $0.30-4.00 liveband is +3.1%/day t+0.50 (mild_conf_liveband.py:94-95,105). So the band decides the headline; it does not merely adjust it. Fix the band in writing as $4.00-$9.90 — what :1349 actually enforces and what the cap arithmetic permits — BEFORE the re-derivation runs, so it cannot be re-selected on the answer. Related: probe_tuner.py:156-163 searches 6 buy-variants x 8 exits = 48 cells per strategy and reports the top 10 by t; the $4-9 winner is a survivor of that search, not a pre-registered constraint.
- A STABILITY GATE, OR WE ARE SAMPLING NOISE. Across four committed tuner reports BULL_DIP's pricey_4_9 -50/80/0.20 cell runs +20.1% t3.75 (09-04) -> +16.0% t3.15 (09-05) -> +11.5% t2.66 (09-07) — a 43% decay in three days of corpus growth — and CONSENSUS_CALLS' pricey_4_9 cell was +28.0% t4.67 on 09-01 and had dropped out of the top-10 entirely by 09-07. Propose: a band's day-mean must not move more than one promotion-floor (3 points) across the last three corpus refreshes, or it does not qualify for the trigger path. Your call whether that gate is worth the strategies it would currently disqualify.
- INSTRUMENT THE REPRICING GATE FOR TWO WEEKS BEFORE TRUSTING ANY CAPTURE NUMBER. The gate at :1349 fail-closes unless the LIVE ask is inside band — so a candidate admitted at an alert ask of $8.50 and repriced to $10.20 is REJECTED, i.e. the engine discards precisely the names where the post-print move it is betting on has already begun; symmetrically a $4.20 that decayed to $3.80 dies on the floor. The archive has no analogue: its entry is a post-print bar close (probe_tuner.py:128) and it books those run-ups in full. In a cohort this skewed the right tail IS the mean, so this will read as 'the edge didn't survive contact' when the real cause is that the engine refused the winners. Log every fail-closed skip with alert ask, live ask and reason, publish the drift distribution of rejects, and if rejects are systematically higher than their alert ask, the comparator must be censored identically. Note also the gate prices off feed=indicative (:1339-1340), a synthesized quote — entry_ref honesty is currently asserted against a non-executable number.
