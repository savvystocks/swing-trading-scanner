#!/usr/bin/env python
import sys
import os

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

if len(sys.argv) > 1 and sys.argv[1] == "tui":
    from src.terminal.tui import main
    main()
else:
    from src.terminal.cli import main
    sys.exit(main(sys.argv[1:]))
