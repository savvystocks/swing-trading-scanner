# MOT COVERAGE MATRIX - 2026-09-07

65 incidents in the matrix: 37 CHECKED, 27 STRUCTURAL, 1 UNCOVERED (the 09-02 red-MOT push, closed by build_today item 2); 10 unhappened classes hunted, all 10 top claims verified true against code (one, the frozen tuner corpus, is actively losing data today); build order lands 9 checks today and 18 consolidated items this week, 6 incidents recorded none-possible, 5 weak proposals killed, 7 duplicate proposals merged.

Rule (BREAKDOWNS.md header): every new breakdown entry names its regression check or states NONE-POSSIBLE.
Full machine-readable matrix: reports/mot_coverage_2026-09-07.json

## Incidents with NO possible automated check (recorded honestly)

- 2026-05-13 Windows Unicode crash - every trade-path process now runs on UTF-8 Linux (GHA ubuntu + Vultr VPS); the cp1252 console class cannot reach the defended surface.
- 2026-05-22 wrong file deployed - the Streamlit Cloud deployment no longer exists; there is no deploy-entry-file surface left to check.
- 2026-06-10 LLM grader silently graded 0 - the subsystem is retired; the class lesson (a checking layer silently dead) is embodied in the absence-alarm and watchdog-of-watchdogs layers that do exist.
- 2026-06-10 VPS root password exposed - a credential pasted into a chat window is not machine-detectable by any repo or VPS check; key-only access with a read-only poller account removed the blast radius instead.
- 2026-07-03 docs described a dead system - prose-vs-code drift has no machine-checkable ground truth; the same-commit graduation rule in CLAUDE.md is the practical control.
- 2026-08-26 precise-replay OOM - interactive research tooling whose failure is loud at run time; the bounded-cache + checkpoint-resume fix is structural and no standing production check is warranted.

## Built 2026-09-07

- 1. Unfreeze the tuner corpus (ACTIVE data loss since Sep 1): compute the bars end-date at runtime in probe_tuner.closes_series (line 38, hardcoded end=2026-08-31) and fade_meta line 59 (end=2026-12-31), add two jsonl_day content rows to freshness_sentinel CHECKS asserting the newest 'day' in probe_tuner_rows.jsonl and glide_fine_rows.jsonl is <=6 trading days behind, and make tuner_apply.main() page-and-abort instead of HOLD when max(row day) exceeds 10 trading days.
- 2. Close the only UNCOVERED hole: commit scripts/pre_push_gate.sh wired via core.hooksPath that runs the four suites plus v11_mot_harness.py with set -o pipefail and no pipes, so a red MOT physically blocks the push instead of being laundered through tail (verified: no hook, no hooksPath exists today).
- 3. Make /halt and /flatten unable to confirm a lie: in telegram_commands._write_flag pull BEFORE writing halt.json, check every subprocess returncode, and send the owner confirmation only when git rev-parse HEAD matches git ls-remote origin main - otherwise reply 'COMMAND FAILED TO PUBLISH - engine has NOT received it'; add a push_sync sentinel row for /home/poller/harvest-snapshots.
- 4. Canary the kill-switch's other end: add a health-check.yml step that clones harvest-snapshots with HARVEST_SNAPSHOTS_DEPLOY_KEY and exits red on failure (verified: the Sunday canary currently never touches SNAP_KEY while v10_lab.yml fail-opens on it), so a dead deploy key pages Sunday instead of making /flatten a permanent no-op.
- 5. Make the sentinel holiday-aware: wire the XNYS calendar into freshness_sentinel.last_expected (skip non-sessions for market-hours rows) and trading_days_behind (count sessions, not weekdays), with a unit test asserting simulated Jul-4 and Labor-Day mornings produce zero stale TRADE rows - protects the pager credibility every other 08:00 alarm depends on.
- 6. Watch the disk that everything lives on: one new 'disk' kind row in freshness_sentinel (shutil.disk_usage('/'), alarm above 85% used, TRADE criticality) plus a 3-line mirror in watchdog_vps.sh so the alarm survives the sentinel itself dying (verified: zero disk checks exist anywhere today).
- 7. Un-drift the failover stand-down coupling: an MOT check extracting the git commit -m string from v10_lab.yml's persist step and asserting it equals the literal 'sandbox lab data [skip ci]' in engine_watch.sh line 69, plus a sentinel mtime row alarming when /home/poller/.engine_watch_failover_mode exists older than 2 hours - prevents permanent twin-exit-engine mode after any commit-message reword.
- 8. Assert stop outranks expiry: MOT dim 3 check that manage_exit(ret=-87%, expiry 2 DTE) returns CLOSE_STOP_LOSS not CLOSE_EXPIRY, plus the FADE-book variant (verified: lines 410/411 test each action only in isolation, the precedence path at sandbox_proactive_lab.py:1115-1126 is unguarded).
- 9. Let the sentinel see the fivek book's corpses: extend the expired_open walker in freshness_sentinel.py (line 198, currently legs-dict-only) to also scan top-level occ and occ_more, so an expired XSP spread stuck OPEN alarms like any other leg.

## Queued this week

- schema_harness hardening in one pass: add a check_alpaca_options step asserting bid/ask/IV/open_interest on a live SPY option latest-quote/snapshot (the exact 04-21 break surface, currently unprobed), a yf.download('^XSP', period='10d') non-empty fresh-last-day probe (fivek settlement source), and make UNREACHABLE on alpaca/uw trip the same Telegram + exit 1 path as DRIFT so a dead key pages Sunday, not Monday's first cycle.
- Atomic spec writes before Friday's tuner window: tmp-file + os.replace in tuner_apply.py and sunday_boundary.py, and after tuner_apply's 'pull --rebase -X ours' re-open fade_book_spec.json and assert the exact tuning block just written survived (compare values) before pushing - page and abort on mismatch.
- reconcile_orphans MOT battery: bare-occ legless record (PUTW/VRP/5K shape) with a matching broker position yields adoptions == [], a fill 10 min old is NOT adopted while 50 min old IS (the 45-min grace at sandbox_proactive_lab.py:2012-2030, currently untested), plus a sentinel row flagging two OPEN records sharing one occ (duplicate-claim detector).
- Persist record-survival guard: in v10_lab.yml snapshot the trade_set_id sets of local and origin/main before the retry loop and after any conflicted merge assert the pushed log's id set is a superset with no status-rank regression (merge_logs._rank), aborting loudly instead of pushing - closes the '-X theirs fallback shreds valid JSON' path at line 122; add an MOT fixture staging a conflict on an unknown file plus the log.
- fivek visibility package: a freshness_sentinel row alarming Friday when the ISO week has no CREDIT_SPREAD_W entry and the week's regime was not BEAR, plus a regime_drill scenario where the short-leg order fails and the wings-only INCOMPLETE record is asserted logged (fivek_probes.py:155-173, happy path only today).
- Watchdog drill suite (one fixture-repo harness, consolidating four proposals): blind ticks 1-3 log without paging, the rollback commit's diff touches only *.py/scripts/ and never the four data files, unchanged-code staleness fires the no-op churn branch with no revert, and the failover path stamps last_cycle_ok and runs a covering cycle on the next tick.
- Out-of-band dead-man for the sentinel's messenger: after every confirmed Telegram delivery freshness_sentinel pings a healthchecks.io URL (the run_poller_vps.sh pattern), so a dead messenger pages through a channel that is not Telegram.
- fade_meta feature-collapse detector: after matrix assembly compute per-feature non-null coverage, write it into model_*.json, and page via its tg() when any feature above 50% coverage in the previous model drops below 25% now.
- Lookahead causality check: recompute persistence features for a sample of stored candidates with inputs hard-truncated at signal_ts and assert equality with stored values, plus a planted future-return canary the Student pipeline must flag rather than score.
- Harvest holiday-barrier test in test_harvest_harvester.py: freeze the clock in a week whose Friday is an XNYS holiday and assert vertical_barrier_ts lands 16:00 ET on the prior trading day, and assert the mcal-import-failure fallback is exercised and flagged.
- integrity_gate additions: on market holidays replace 'SKIP (market holiday)' with an assertion that zero candidates were logged that day, and add a nightly reconcile of yesterday's executed legs in proactive_sandbox_logs.json against executed-tier rows in harvest.db (occ + day) failing RED on any one-sided record - the WULF-loss signature.
- Promote data_integrity_audit.py from one-off report to a scheduled weekly gate with exit 1 + Telegram, adding a non-null-rate assertion on IV/NBBO columns for each week's new contracts_daily rows (the 07-28 defect passes every freshness check today).
- GHA quota meter: in run_weekly.py's ops-telemetry block page Telegram and mark the report RED when the monthly runner-minutes projection exceeds 90% of 3,000.
- grand_retest funnel guard: if universe > 0 and true-trigger trades scored == 0, print the funnel dict and exit 1 so a zero-score run can never pass as complete.
- Probe-scope absence alarm: daily_digest counts today's book=PROBE records and fires _notify when probes logged zero entries on a market day - mirrors the 08-04 zero-entry alarm at probe scope, where a fade-entries-present drought currently pages nothing.
- Rotation and court suite additions (09-01 class, consolidated): MOT check that an all-never-entering roster invokes enter_proactive_set at most 6 times, rotation-reachability over a simulated week of (day,hour) seeds with EXEC_BASELINE always first, and a court unit check that a _W leg with 1 fill/week never breaches the >=3/week floor.
- Wednesday blind-spot MOT check: stub _alpaca_atm_iv to None for the CAL_FRONT_DTE window and 55.0 for (7,18), assert iv_front == 55.0 source alpaca, plus a constant-scan asserting every DTE window in the sensor file spans >= 7 days.
- Transcript-store insurance: weekly cron rsyncing ~/.claude/projects/<encoded>/ into the private harvest-snapshots repo plus a freshness_sentinel git_commit row, turning an unrecoverable purge into a restorable one that alarms when the backup stops.
